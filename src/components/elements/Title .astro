---
interface Props {
  text?: string;
  className?: string;
  [key: string]: any;
}

const { text, className, ...data } = Astro.props;
---

<h1 set:html={text} class:list={["c-title", className]} {...data} />
